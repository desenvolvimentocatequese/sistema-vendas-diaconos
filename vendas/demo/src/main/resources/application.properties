spring.application.name=demo

# Database Configuration (arquivo local: dados persistem após shutdown)
# Pasta ./data criada sob o diretório de trabalho ao subir o app (ex.: vendas/demo/data/)
spring.datasource.url=jdbc:postgresql://dpg-d7si6uugkk3c73d88s7g-a.oregon-postgres.render.com:5432/sistema_vendas_ehsf?sslmode=require
spring.datasource.username=sistema_vendas_ehsf_user
spring.datasource.password=zbLdhlVqjlMBf6AdcR67Sy8WxyrH97vc
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
api.security.token.secret=my-super-secret-key-that-must-be-very-long-to-work-properly-with-hs256-algorithm
api.security.token.expiration=86400000

# Frontend URL
app.frontend.url=http://localhost:3000

# Server Configuration (Render e outros PaaS injetam PORT)
server.port=${PORT:9090}

# Actuator
management.endpoints.web.exposure.include=health,metrics
management.endpoint.health.show-details=always
